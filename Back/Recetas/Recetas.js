import fs from "fs";
import {onEvent,startServer} from "soquetic"
function DatosUsuario(datos){
    let altura = datos.altura;
    let peso = datos.peso;
    let edad = datos.edad;
}
function Receta(datos){
if altur
}

function receta(data) {


}
onEvent("receta", receta)